# Khi phát triển frontend và backend ở hai cổng khác nhau:
VITE_API_BASE_URL=http://localhost:8080/api

# Khi production phục vụ frontend và API cùng domain, có thể bỏ biến này.
# Frontend sẽ tự dùng đường dẫn tương đối /api.
